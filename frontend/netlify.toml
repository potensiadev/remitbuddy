[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "out"  # .next → out으로 변경

[build.environment]
  NEXT_PUBLIC_API_URL = "https://sendhome.onrender.com"

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404