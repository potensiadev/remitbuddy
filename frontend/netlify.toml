[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "out"  # .next가 아닌 out 디렉토리

[build.environment]
  NEXT_PUBLIC_API_URL = "your-api-url"

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404